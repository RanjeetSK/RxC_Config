
spring.datasource.url=${vcap.services.initial-setup-service.credentials.jdbcUrl}
spring.datasource.username=${vcap.services.initial-setup-service.credentials.username}
spring.datasource.password=${vcap.services.initial-setup-service.credentials.password}
#spring.h2.console.settings.web-allow-others=true
#spring.jpa.hibernate.ddl-auto: create
